# 2026-02-23 Skill update routing automation

- Added automated skill update decision routing (`UPDATE_EXISTING` / `ADD_NEW` / `NO_CHANGE`) driven by changed files.
- Added deterministic pre-commit guard for skill updates and dedicated regression tests.
- Synced local skill inventory and updated agent workflow docs (`AGENTS.md`, `docs/DEV_WORKFLOW.md`, `docs/session_handoff.md`, `plans/PLANS.yaml`).
